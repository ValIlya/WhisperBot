# WhisperBot

Bot for chatting with LLM

```bash
cp env.template .env
poetry install
```
Fill up env variables  
Run bot via
```bash
whisperbot
```

### TODO:
 - support audio parsing
 - async 
    - read-write to storage (Motor)
    - llm chats
 - tests would be nice
